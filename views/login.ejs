<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <title>Supermarket App - Login</title>

  <style>
    :root{ --brand:#0d6efd; --muted:#6c757d; --nav-height:70px }
    body {
      background: linear-gradient(180deg,#f6fbf6 0%, #eaf7ea 60%);
      background-image: url('/images/background.png');
      background-repeat: no-repeat;
      background-position: center 65%;
      background-size: cover;
      min-height: 80vh;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      padding: 2rem 0 0 0; /* top padding will be set below for fixed navbar */
    }

    .login-box {
      background: #fff;
      border-radius: 16px;
      padding: 32px;
      width: 100%;
      max-width: 420px;
      box-shadow: 0 8px 30px rgba(18,38,63,0.08);
      border: 1px solid rgba(13,110,253,0.06);
    }

    /* Page content wrapper keeps navbar at top while centering the box */
    .page-content {
      min-height: calc(100vh - var(--nav-height)); /* leave space for navbar */
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    /* Keep space for fixed navbar so content isn't hidden */
    body { padding-top: var(--nav-height); }

    /* ensure navbar is above content */
    .navbar.fixed-top { z-index: 1030; box-shadow: 0 2px 6px rgba(0,0,0,0.06); }

    .brand-sm {
      display:block; text-align:center; margin-bottom:8px; color:var(--brand); font-weight:700; font-size:20px;
    }

    .login-title { font-size: 26px; font-weight:700; margin-bottom:12px; text-align:center }
    .login-sub { font-size:14px; color:var(--muted); text-align:center; margin-bottom:18px }

    .login-input { border-radius:10px; padding:12px; height:48px; font-size:15px }

    label { font-size:14px; font-weight:600 }

    .login-btn { border-radius:10px; padding:10px 18px; font-size:16px }

    .helper-links { font-size:14px; color:var(--muted); margin-top:12px }

    @media (max-width:575px){
      .login-box{ padding:20px; margin:0 8px }
      .login-title{ font-size:22px }
    }
  </style>
</head>

<body>

  <!-- NAVBAR -->
   <nav class="navbar navbar-expand-sm bg-dark navbar-dark py-3 px-4 fixed-top">
     <div class="container-fluid">
       <a class="navbar-brand fw-bold" href="/" style="font-size: 22px;">Supermarket</a>
       <ul class="navbar-nav ms-auto">
         <li class="nav-item"><a class="nav-link" href="/about">About us</a></li>
         <li class="nav-item"><a class="nav-link" href="/help">Need help?</a></li>
       </ul>
     </div>
   </nav>

  <!-- LOGIN BOX -->
  <div class="page-content">
    <div class="login-box">
    <span class="brand-sm">Supermarket App</span>
    <h2 class="login-title">Welcome back</h2>
    <div class="login-sub">Sign in to continue to your account</div>

    <% if (errors && errors.length > 0) { %>
      <div class="alert alert-danger">
        <% errors.forEach(function(error) { %>
          <p><%= error %></p>
        <% }); %>
      </div>
    <% } %>

    <% if (messages && messages.length > 0) { %>
      <div class="alert alert-success">
        <% messages.forEach(function(message) { %>
          <p><%= message %></p>
        <% }); %>
      </div>
    <% } %>

    <form action="/login" method="POST">


      <div class="mb-3">
        <label for="email">Email</label>
        <input id="email" type="email" name="email" class="form-control login-input mb-2" required>
      </div>

      <div class="mb-2">
        <label for="password">Password</label>
        <input id="password" type="password" name="password" class="form-control login-input mb-2" required>
      </div>

      <div class="d-flex justify-content-between align-items-center mt-3">
        <div class="helper-links">Don’t have an account? <a href="/register">Sign up</a></div>
        <button type="submit" class="btn btn-primary login-btn">Login</button>
      </div>

    </form>
    </div>
  </div>

</body>
</html>
